# 🔧 bidernet v2.4.7-php - תיקון סנכרון לקוח↔אדמין

## 🐛 הבעיה שתוקנה

**הלקוח ערך פוסט, ראה "נשמר בהצלחה", אבל האדמין לא ראה את השינוי**

הסיבה: ה-frontend היה שולח שדה בשם `history` ו-`messages`, אבל ה-DB משתמש בשמות `editHistory` ו-`chatMessages`. ה-API לא ידע להמיר ביניהם!

## ✅ הפתרון ב-v2.4.7

### 1. תיקון api.php - מיפוי שדות
- **`history` ↔ `editHistory`** - העריכות מהלקוח עכשיו נשמרות נכון
- **`messages` ↔ `chatMessages`** - גם הודעות נשמרות נכון
- ה-API מקבל את שני הפורמטים ומחזיר את שניהם (תאימות מלאה)

### 2. כפתור רענון בכותרת האדמין
- 🔄 **"רענן"** - בכותרת ליד "מיתוג" ו-"יציאה"
- לחיצה תטען את כל הפוסטים מחדש מהשרת
- שימושי לראות שינויים שהלקוח עשה תוך כדי השיחה

## 🚀 התקנה - **חשוב!**

יש 2 קבצים לעדכן (חובה את שניהם!):

### 1. העלה את הקבצים החדשים ל-`/public_html/app.bidernet.co.il/`:
- ✅ **`app.js`** (חדש - v2.4.7)
- ✅ **`api.php`** ⭐ **חובה! בלי זה הסנכרון לא יעבוד**

⚠️ **אל תחליף את `config.php`** - השאר את הסיסמה הקיימת!

### 2. רענון חזק
**Ctrl+Shift+R**

## 🧪 בדיקה

ב-Console (F12):
```
🎯 bidernet Content Calendar v2.4.7-php
🔧 FIX: Client edits now sync to admin properly
```

### תרגיל בדיקה:
1. **כלקוח** - פתח פוסט → "ערוך פוסט" → ערוך → שמור
2. **כאדמין** - לחץ על כפתור 🔄 **"רענן"** בכותרת
3. אמור לראות את התוכן החדש!
4. תוכל גם לראות ב-history של הפוסט: "✏️ הלקוח ערך את תוכן הפוסט"
